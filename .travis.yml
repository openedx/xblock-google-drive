language: python
python:
  - "2.7"

env:
  - TOXENV=django18
  - TOXENV=django110
  - TOXENV=django111
  - TOXENV=quality

before_install:
    - "export DISPLAY=:99"
    - "sh -e /etc/init.d/xvfb start"

addons:
  firefox: '36.0'

install:
  - pip install -r requirements/travis.txt

script:
  - tox

after_success: coveralls
